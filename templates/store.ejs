<div class="row">
	<div class="col s12">
		<ul class="tabs tabs-fixed-width">
			<li class="tab"><a href="#" onclick="apretaste.send({command: 'AMULETOS'})">Inventario</a></li>
			<li class="tab"><a href="#" class="active">Druida</a></li> 
		</ul>

		<p><span class="green-text">Usted tiene <b>§<%= credits %></b></span><br>
		El Druida le canjea amuletos, que usted puede equipar para usar sus poderes.</p>

		<ul class="collection">
			<% amulets.forEach(function(item) { %>
				<li class="collection-item avatar">
					<i class="material-icons circle green"><%= item.icon %></i>
					<span class="title"><b><%= item.name %></b></span>
					<p class="green-text">
						<% if(item.duration > 0) { %>
							Efecto <%= item.duration %>h
						<% } else { %>
							Efecto ilimitado
						<% } %>

						<span class="black-text">&middot;</span>
						§<%= item.price %>
					</p>

					<p class="desc"><%= item.description %></p>

					<a href="#!" class="secondary-content" onclick="openModalBuy('<%= item.code %>','<%= item.name %>','<%= item.price %>')">
						<i class="material-icons green-text">shopping_cart</i>
					</a>
				</li>
			<% }) %>
		</ul>

		<p class="note grey-text">Una vez canjeados, el tiempo de efectividad del amuleto empezará a descontar.</p>
	</div>
</div>

<!-- MODALS -->

<div id="modal" class="modal">
	<div class="modal-content">
		<p>¿Esta seguro que desea canjear el amuleto <span id="modalName"></span> por §<span id="modalPrice"></span> créditos? Esta acción es irrevocable.</p>
	</div>
	<div class="modal-footer">
		<a href="#!" class="modal-close waves-effect waves-light btn-flat">Cancelar</a>
		<a onclick="buy()" class="modal-close waves-effect waves-green btn-flat green-text">Canjear</a>
	</div>
</div>