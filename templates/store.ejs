<div class="row">
	<div class="col s12">
		<ul class="tabs tabs-fixed-width">
			<li class="tab"><a href="#" onclick="apretaste.send({command: 'AMULETOS'})">Inventario</a></li>
			<li class="tab"><a href="#" class="active">Tienda</a></li> 
		</ul>

		<p><b>Usted tiene §<%= credits %></b><br>
		Canjee y active amuletos para ganar poderes especiales. Una vez canjeado, el tiempo de efectividad empezará a descontar.</p>

		<ul class="collection">
			<% amulets.forEach(function(item) { %>
				<li class="collection-item avatar">
					<i class="material-icons circle"><%= item.icon %></i>
					<span class="title"><b><%= item.name %></b></span>
					<p class="grey-text">
						<% if(item.duration > 0) { %>
							Efectivo por <%= item.duration %> horas
						<% } else { %>
							Efecto ilimitado
						<% } %>
					</p>
					<p class="desc"><%= item.description %></p>
					<p><a href="#!" onclick="openModalBuy('<%= item.code %>')">Canjear por §<%= item.price %></a></p>
				</li>
			<% }) %>
		</ul>
	</div>
</div>

<!-- MODALS -->

<div id="modal" class="modal">
	<div class="modal-content">
		<p>¿Esta seguro de realizar este canje? Esta acción es irrevocable.</p>
	</div>
	<div class="modal-footer">
		<a href="#!" class="modal-close waves-effect waves-light btn-flat">Cancelar</a>
		<a onclick="buy()" class="modal-close waves-effect waves-green btn-flat green-text">Canjear</a>
	</div>
</div>